import 'package:flutter/material.dart';

class BeautyRecommendationsScreen extends StatelessWidget {
  const BeautyRecommendationsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Makeup & Hairstyle Recommendations Screen'),
      ),
    );
  }
}